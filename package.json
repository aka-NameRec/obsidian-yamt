{
  "name": "yamt",
  "version": "1.3.20260428",
  "private": true,
  "license": "Unlicense",
  "author": "aka.NameRec@gmail.com",
  "scripts": {
    "dev": "esbuild src/main.ts --bundle --outfile=main.js --external:obsidian --format=cjs --target=es2018 --platform=browser --watch",
    "build": "esbuild src/main.ts --bundle --outfile=main.js --external:obsidian --format=cjs --target=es2018 --platform=browser --minify",
    "check": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@types/node": "^22.7.5",
    "esbuild": "^0.24.0",
    "obsidian": "^1.5.0",
    "typescript": "^5.6.3",
    "vitest": "^2.1.4"
  },
  "dependencies": {
    "js-yaml": "^4.1.0"
  }
}
